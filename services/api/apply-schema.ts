import { Pool } from 'pg'; import * as fs from 'fs'; import * as path from 'path'; const envPath = path.join(process.cwd(), '.env'); if (fs.existsSync(envPath)) { const envContent = fs.readFileSync(envPath, 'utf-8'); envContent.split('\n').forEach(line => { const match = line.match(/^\s*([\w.-]+)\s*=\s*(.*)?\s*$/); if (match) { const key = match[1]; let value = match[2] || ''; if (value.startsWith('\'') && value.endsWith('\'')) value = value.slice(1, -1); process.env[key] = value; } }); } const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false }}); pool.query('ALTER TABLE medicines ADD COLUMN IF NOT EXISTS description text; ALTER TABLE medicines ADD COLUMN IF NOT EXISTS image_url text; ALTER TABLE medicines ADD COLUMN IF NOT EXISTS composition text; ALTER TABLE medicines ADD COLUMN IF NOT EXISTS dosage_form text; ALTER TABLE medicines ADD COLUMN IF NOT EXISTS manufacturer text;').then(() => { console.log('Schema updated successfully'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })
